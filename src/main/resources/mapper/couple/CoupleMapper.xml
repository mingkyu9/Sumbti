<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="app.sum.bti.couple.mapper.CoupleMapper">

    <select id="getCoupleList"  resultType="coupleList">


        select user_id,
        user_mbti,
        user_gender,
        user_nick,
        (select count(*) from like_list lk where lk.like_to  = u.user_id) as like_count
        from user_info u
    </select>

    <select id="getCoupleListSelect"  resultType="coupleList">

        select user_id,
        user_mbti,
        user_gender,
        love_mbti,
        hate_mbti,
        user_nick
        from user_info
    </select>



</mapper>
